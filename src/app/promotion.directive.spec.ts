import { PromotionDirective } from './promotion.directive';

describe('PromotionDirective', () => {
  it('should create an instance', () => {
    const directive = new PromotionDirective();
    expect(directive).toBeTruthy();
  });
});
